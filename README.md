this project relies on Pipenv to manage dependencies. you will need both pipenv and python installed on your machine

To run this project, naviage to the repository and run:
    pipenv run python main.py